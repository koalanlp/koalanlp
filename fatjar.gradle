task fatJar(type: Jar) {
    classifier "assembly"
    from {
        fileTree(dir: 'lib', include: '*.jar').collect { zipTree(it) }
    }
    with jar
}

publishing {
    publications {
        KoalaNLP(MavenPublication) {
            artifact fatJar
        }
    }
}
